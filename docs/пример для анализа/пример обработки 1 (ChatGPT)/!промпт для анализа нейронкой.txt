У меня есть таблица «Заработная плата ПАО „Роснефтегоблгаз“ за декабрь 2023 года».
В таблице по каждому сотруднику указаны: подразделение, ФИО, группа должностей, стаж (лет), оклад (₽/мес), премия (%), регион.
Сформируй аналитическую справку по данным таблицы. Требования к результату:
1.	Краткое резюме (3–6 предложений): ключевые выводы и наиболее заметные отклонения.
2.	Экстремумы и лидеры:
•	кто имеет самый высокий оклад (ФИО, подразделение, группа должностей, регион, стаж, оклад);
•	кто имеет самый низкий оклад (те же поля);
•	кто имеет самую высокую премию, % (те же поля);
•	кто имеет самую низкую премию, % (те же поля).
3.	Рейтинги:
•	топ-5 сотрудников по окладу (убывание);
•	топ-5 сотрудников по премии, % (убывание).
4.	Сводные показатели по всей таблице:
•	средний, медианный, минимальный и максимальный оклад;
•	средняя, медианная, минимальная и максимальная премия (%);
•	средний стаж.
5.	Аналитика по разрезам:
•	по подразделениям: средний оклад и средняя премия (%), количество сотрудников; вывести 3 подразделения с самым высоким средним окладом и 3 — с самым низким;
•	по группам должностей: средний оклад и средняя премия (%), количество сотрудников; выделить группы с наибольшим и наименьшим средним окладом;
•	по регионам: средний оклад и средняя премия (%), количество сотрудников; отметить регионы-лидеры и аутсайдеры.
6.	Зависимости и наблюдения:
•	есть ли связь между стажем и окладом (качественное заключение + примеры из таблицы);
•	есть ли связь между окладом и премией (%);
•	перечисли 3–7 самых заметных аномалий/неожиданных случаев (например, высокий оклад при низкой премии, высокий стаж при низком окладе и т.п.).
Форматируй ответ структурированно, с подзаголовками и списками. Все выводы делай только на основании данных таблицы, без предположений вне таблицы.

В прилагаемое таблице встречаются токены вида [[...]]. Это псевдоанонимизированные значения: они заменяют исходные данные и расшифровываются только по отдельному JSON-словарю соответствий.
Жёсткое правило: не изменяй токены [[...]] ни при каких условиях.

(1) Токен — это подстрока от [[ до ]] целиком. Запрещено разрывать токен переносом строки или вставлять внутрь токена пробелы/переносы.
(2) Не меняй внутри токена ни одного символа: буквы/цифры/знаки, регистр, длину.
(3) Не добавляй и не удаляй символы внутри токена. Не разбивай токен на части и не объединяй токены между собой.
(4) Не пытайся расшифровать токены и не придумывай исходные значения.
(5) Запрещено создавать новые токены вида [[...]]. Используй только те токены, которые даны во входных данных.
(6) Если нужно сослаться на значение — используй токен ровно как он дан, целиком, включая [[ и ]]. Если токен встречается несколько раз — считай, что это одно и то же значение.
(7) Если в ответе используются токены — выводи ответ простым текстом (без Markdown-таблиц, код-блоков и ссылок), чтобы не исказить токены.
(8) Перед отправкой ответа проверь, что каждый токен в твоём ответе совпадает с токеном из входных данных символ-в-символ.